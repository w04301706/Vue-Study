# Vue-Study
vue学习用的项目，要先安装nodejs环境
启动步骤：
1、启动服务器(vue_api_server)：
    1)执行mydb.sql，将数据初始化到数据库中
    2）修改vue_api_server/default.json,配置正确的数据库连接信息
    3）运行 node app.js命令，启动服务器
2、启动客户端（vue_shop)：
    1、配置main-dev.js中的服务器信息
    2、按照正常方式开发调试即可（建议用vue ui）
